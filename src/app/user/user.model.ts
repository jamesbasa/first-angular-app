export type User = {
  id: string;
  name: string;
  avatar: string;
};

// interface, as opposed to type, is restricted to defining object types
// export interface User {
//   id: string;
//   name: string;
//   avatar: string;
// }
